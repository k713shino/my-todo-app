version: '3.8'

services:
  # PostgreSQL（ローカル用）- AWS使用時は不要
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: todo-postgres
  #   environment:
  #     POSTGRES_DB: todoapp
  #     POSTGRES_USER: todouser
  #     POSTGRES_PASSWORD: todopass123
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - todo-network

  # Next.js アプリケーション
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-app
    ports:
      - "3000:3000"
    environment:
      # AWS RDS PostgreSQL接続
      - DATABASE_URL=postgresql://todouser:Hinewfrost3@my-todo-app-db.cfcy8c2wq7lr.ap-northeast-1.rds.amazonaws.com:5432/todoapp
      - NEXTAUTH_SECRET=jBu9vAKuRwvEgnjuUbNEaOdByDAuHRQm1HQNmqwbymY=
      - NEXTAUTH_URL=http://localhost:3000
    networks:
      - todo-network

  # pgAdmin (データベース管理ツール)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: todo-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@todo.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - todo-network

# volumes:
#   postgres_data:  # AWS使用時は不要

networks:
  todo-network:
    driver: bridge
